import Testing
@testable import ReticulumRouting

// ReticulumRouting Module Tests

@Suite("ReticulumRouting")
struct ReticulumRoutingTests {
    // Tests will be added here as features are implemented.
    // First features to test:
    //   - RoutingTable path insert / lookup
    //   - Path expiry
    //   - AnnounceManager signature validation
    //   - AnnounceManager deduplication
}
