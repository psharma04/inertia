import Testing
@testable import ReticulumCore

// ReticulumCore Module Tests

@Suite("ReticulumCore")
struct ReticulumCoreTests {
    // Tests will be added here as features are implemented.
    // First features to test:
    //   - ReticulumStack lifecycle (start / stop)
    //   - Destination hash generation (binary equality with Python)
    //   - Announce propagation integration
}
